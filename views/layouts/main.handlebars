<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Rack Em Up</title>
        <!-- CSS only -->
        
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor" crossorigin="anonymous">
<!-- add easay change to bootstrap css here-->
<style>
.nav_image{
    background-image: url(/stylesheets/gunwood.avif);
    background-size: cover;
    background-repeat: no-repeat;
    min-height: 300px;
    background-position: center center;
}
body{
    background-color: bisque;
}
</style>
    </head>

    <body>
       <nav class="navbar navbar-expand-md navbar-light bg-dark nav_image">
                <section class="navItems">
                <h1>
                    <a href="/" class="btn btn-link btn-outline-info" role="button">Rack Em Up</a>
                </h1>
                
                <a class="btn btn-link btn-outline-danger" role="button" href="/dashboard">Dashboard</a>
                     <a role="button" class="btn btn-link btn-outline-success" href="/">Home</a>
                    {{#if loggedIn}}
                    <a role="button" class="btn btn-link btn-outline-primary" href="/" id="logout">Logout</a>
                    {{else}}
                    <a role="button" class="btn btn-link btn-primary btn-sm" href="/login">Login</a>
                    {{/if}}
                </section>
                
            </nav>
            <br>
            <main>
                {{{ body }}}
            </main>
            <footer>
                Thanks for visiting!
            </footer>
       
        {{#if loggedIn}}
        <script src="/javascript/logout.js"></script>
        {{/if}}
    </body>
</html>